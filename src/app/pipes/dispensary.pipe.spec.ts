import { DispensaryPipe } from './dispensary.pipe';

describe('DispensaryPipe', () => {
  it('create an instance', () => {
    const pipe = new DispensaryPipe();
    expect(pipe).toBeTruthy();
  });
});
